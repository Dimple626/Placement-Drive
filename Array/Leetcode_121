class Solution {
    public int maxProfit(int[] arr) {
        int n=arr.length;
        int maxsofar=0;
        for(int i=0;i<n-1;i++){
            if(arr[i]<arr[i+1]){
                maxsofar=Math.max(maxsofar,arr[i+1]);
            }

        }
        return maxsofar;
    }
}
